apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ke.certsSecret.name }}
  namespace: {{ .Release.Namespace}}
data:
  aqua_ke.crt: {{ template "serverCertificate" . }}  # place server cert
  aqua_ke.key: {{ template "serverKey" . }}  # place server key